#!/bin/bash

# This file calls the principal file in $SHAZAM,
# which makes all the processings. But if it don't
# find the file, echo a error message

# App's path
SHAZAM=/usr/lib/shazam/shazam.py

if [ -e $SHAZAM ]
then
	$SHAZAM $@
else
	echo 'shazam: error:' $SHAZAM 'was not found!!'
fi
